{
  "name": "mccoreclient-cli",
  "version": "0.0.1",
  "description": "This project makes it easier for users to create and upload packages to MCCoreClient.",
  "main": "lib/index.js",
  "bin": {
    "mccoreclient": "./lib/src/index.js"
  },
  "scripts": {
    "cli": "ts-node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf lib && tsc"
  },
  "author": "mccoreclient",
  "repository": {
    "type": "git",
    "url": "https://github.com/MCCoreClient/cli"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.15.17",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "commander": "^13.1.0",
    "fast-glob": "^3.3.3",
    "firebase": "^11.7.1",
    "inquirer": "^12.6.0",
    "rimraf": "^6.0.1"
  }
}
